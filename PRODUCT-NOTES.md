# Adjudex Product Notes

Current product direction is real-data only.

The app requires a configured backend API, wallet connector, chain RPC, and deployed contracts for user-facing balances, transactions, history, markets, leaderboards, agents, and resolutions.

Use `README.md` and `adjudex-tab-functionality-architecture.md` as the active implementation references.
